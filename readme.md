
# Эволюция — Игра

Запуск локально:

```
cd client
npm install
npm run dev
```

Сборка и сервер:

```
cd client && npm run build
cd ../server && node index.js
```

Готово к Railway и другим Node.js платформам.
